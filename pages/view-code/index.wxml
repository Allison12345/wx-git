<!-- @format -->

<my-page>
  <view-code-item
    wx:for="{{ codeFiles }}"
    wx:key="*this"
    label="{{ item.name }}"
    url="{{ item.url }}"
    isLink="{{ item.type === 'dir' }}"
    bind:tap="onTap"
  ></view-code-item>
</my-page>
